import { Field } from '../../src';

describe('`Field` component', () => {
  it('should be defined', () => {
    expect(Field).toBeDefined();
  });

  it('should throw an error', () => {
    expect(Field).toThrowError();
  });
});
