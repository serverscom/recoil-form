import { FieldArray } from '../../src';

describe('`FieldArray` component', () => {
  it('should be defined', () => {
    expect(FieldArray).toBeDefined();
  });

  it('should throw an error', () => {
    expect(FieldArray).toThrowError();
  });
});
