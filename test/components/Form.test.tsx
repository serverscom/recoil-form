import { Form } from '../../src';

describe('`Form` component', () => {
  it('should be defined', () => {
    expect(Form).toBeDefined();
  });

  it('should throw an error', () => {
    expect(Form).toThrowError();
  });
});
